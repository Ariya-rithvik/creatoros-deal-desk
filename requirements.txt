fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
playwright>=1.44        # then: python -m playwright install chromium
httpx>=0.27
beautifulsoup4>=4.12
cedarpy>=4.12           # AWS Cedar policy engine; optional - without it the built-in guard still applies
pytest>=8
